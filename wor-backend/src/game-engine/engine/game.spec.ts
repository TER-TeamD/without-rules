import {Game} from "./game";


describe("Deck initialization ", () => {
    test("Should return 104 cards", () => {
        // const game: Game = new Game();

        // expect<number>(game.deckCards.length).toEqual(104)
    })
})
